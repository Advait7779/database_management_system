{
    admin off
    auto_https off
}

:{$PORT:80} {
    encode gzip

    log {
        output stdout
    }

    # Proxy all /api/* requests to the backend container internally
    handle /api/* {
        reverse_proxy qg84w84wo0kgc0swockg48ww:5001 {
            header_up Host {host}
            header_up X-Real-IP {remote_host}
            header_up X-Forwarded-For {remote_host}
        }
    }

    # Serve static React build with SPA fallback
    handle {
        root * /app/dist
        try_files {path} /index.html
        file_server
    }
}
